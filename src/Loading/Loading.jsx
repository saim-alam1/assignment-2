const Loading = () => {
  return (
    <div className="max-w-300 mx-auto flex flex-col items-center justify-center min-h-screen">
      <span className="loading loading-spinner loading-xl"></span>
    </div>
  );
};

export default Loading;
